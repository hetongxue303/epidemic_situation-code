server:
  port: 8080
spring:
  application:
    name: es
  config:
    import:
      - application-web.yml
      - application-log.yml
      - application-redis.yml
      - application-datasource.yml
      - application-mybatisPlus.yml